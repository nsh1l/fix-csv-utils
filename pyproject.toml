[project]
name = "fix-newline-in-csv"
version = "0.1.0"
description = "CSV内のフィールド内改行を除去し、文字コードを自動判別してUTF-8に変換するツール"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.2",
    "chardet>=5.0.0",
    "streamlit>=1.40.0",
]
